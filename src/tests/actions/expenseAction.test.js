import {addExpense, removeExpence} from "../../action/expencesActions";

test("Add Expense",()=>{
   // const first = addExpense({description:'my test Description',note:'',amount:45678,createdAt:0});
const remove = removeExpence("678434");
   expect(remove).toEqual(remove)
})
